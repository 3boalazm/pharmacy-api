DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pharmacy?schema=public
JWT_SECRET=change-me-in-production
PORT=3000
CORS_ORIGIN=http://localhost:3001

# Optional — enables R-1 single-use override tokens, R-2 rate limiting, R-3 read caches.
# Unset = degraded mode: API stays fully correct (caching/limits off, overrides JWT-only).
REDIS_URL=redis://localhost:6379
